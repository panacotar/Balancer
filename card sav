<% if !@orders.nil? %>
  <% @orders.each do |order|%>
    <%= render "campaign_details", order: order %>
  <% end %>
<% else %>
  <h6>No contribuition under your belt at the moment.</h6>
    <% end %>

PROJECT 1
